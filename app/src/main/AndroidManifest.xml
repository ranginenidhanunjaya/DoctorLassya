<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🎙️ Microphone for voice input & wake word -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 📞 Emergency 108 calling -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- 🌐 API calls to Gemini & ElevenLabs -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 📷 Camera for injury/rash analysis -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 📳 Vibration for emergency alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 🔔 Foreground service for always-on wake word -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- 💾 Read/write for local DB -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".LaasyaApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.DoctorLaasya"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 🏠 Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🎙️ Wake Word Foreground Service -->
        <service
            android:name=".service.LaasyaWakeWordService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- 🔊 TTS Streaming Service -->
        <service
            android:name=".service.LaasyaTTSService"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
